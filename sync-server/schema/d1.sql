-- Memry Sync Server D1 Schema
-- This file contains the database schema for the sync server.
-- Run: wrangler d1 execute memry-sync --file=./schema/d1.sql

-- Schema will be added in Phase 2 (T014-T017)
-- Tables:
--   - users (user accounts with KDF salt and key verifier)
--   - devices (linked devices)
--   - linking_sessions (QR code device linking)
--   - sync_items (encrypted item metadata)

-- Placeholder - schema will be implemented in Phase 2
SELECT 1;
